{
  "scripts": {
    "dev": "next dev",
    "build": "next build",
    "start": "next start",
    "lint": "next lint",
    "prisma:generate": "prisma generate",
    "prisma:migrate": "prisma migrate dev",
    "db:push": "prisma db push",
    "sync-users": "ts-node --compiler-options '{\"module\":\"CommonJS\"}' scripts/sync-users.ts"
  }
}

